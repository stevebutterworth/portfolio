<nav class="sticky top-0 z-20 flex flex-wrap items-center justify-between gap-x-6 gap-y-2 border-b border-line bg-page/85 px-5 py-5 backdrop-blur md:px-10">
  <a href="/" class="flex items-center gap-2 font-display font-semibold">
    <span class="font-mono font-bold text-coral">//</span> Steve Butterworth
  </a>
  <div class="flex gap-4 font-mono text-xs uppercase tracking-widest md:gap-6">
    <a href="/" class="<%= current_page?("/") ? "text-coral" : "text-ink" %>">Work</a>
    <a href="/cv" class="<%= request.path.start_with?("/cv") ? "text-coral" : "text-ink" %>">CV</a>
    <a href="/writing" class="<%= request.path.start_with?("/writing") ? "text-coral" : "text-ink" %>">Writing</a>
    <a href="/contact" class="<%= request.path.start_with?("/contact") ? "text-coral" : "text-ink" %>">Contact</a>
  </div>
</nav>
