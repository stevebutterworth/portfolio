<nav class="sticky top-0 z-20 mx-auto flex w-full max-w-7xl flex-wrap items-center justify-between gap-x-6 gap-y-2 border-b border-line bg-page/85 px-5 py-5 backdrop-blur md:px-10">
  <a href="/" class="flex items-center gap-2 font-display font-semibold">
    <span class="font-mono font-bold text-accent">//</span> Steve Butterworth
  </a>
  <div class="flex gap-4 text-xs font-semibold uppercase tracking-widest md:gap-6">
    <a href="/" class="<%= current_page?("/") ? "text-accent" : "text-ink" %>">Work</a>
    <a href="/cv" class="<%= request.path.start_with?("/cv") ? "text-accent" : "text-ink" %>">CV</a>
    <a href="/contact" class="<%= request.path.start_with?("/contact") ? "text-accent" : "text-ink" %>">Contact</a>
  </div>
</nav>
